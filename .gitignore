# ── Secrets — NEVER commit ───────────────────────────────────
.env
.env.local
.env*.local
service_account.json
*.serviceaccount.json
*-service-account.json

# ── Next.js / Node ───────────────────────────────────────────
/node_modules
/.next/
/out/
/build
next-env.d.ts
.vercel
*.tsbuildinfo

# LibreOffice temp/lock artifacts (from résumé PDF generation)
public/*.tmp
public/.~lock*

# ── OS / editor ──────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Python (legacy FastAPI site in /legacy-fastapi-site) ─────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.log
.ipynb_checkpoints
.mypy_cache/
.ruff_cache/
.pytest_cache/
